package org.fanteract.enumerate

enum class Status {
    ACTIVATED,
    DELETED,
}

enum class AlarmStatus {
    CREATED,
    DELETED,
    UPDATED,
}

enum class ChatroomJoinStatus{
    JOIN,
    LEAVE,
}